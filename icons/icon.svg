<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 128 128">
  <!-- Background circle -->
  <circle cx="64" cy="64" r="64" fill="#18181b"/>
  <!-- 译 (translate) in pink, lower-right, drawn first so A sits on top -->
  <!-- 说 in white, upper-left, drawn first so A sits on top -->
  <text x="6" y="82" font-family="Noto Sans CJK SC, Noto Serif CJK SC, serif" font-size="78" font-weight="bold" fill="white">说</text>
  <!-- A in purple, lower-right, on top -->
  <text x="44" y="116" font-family="Arial Black, Arial, sans-serif" font-weight="900" font-size="78" fill="#D946EF">A</text>
</svg>
